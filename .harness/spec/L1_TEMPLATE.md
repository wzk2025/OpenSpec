# SPEC 模板

> 复制此模板创建新的 SPEC 文档。
> 复制时将 `{占位符}` 替换为实际内容，保留 YAML frontmatter 格式。

---
spec_id: SPEC-{序号}
module: {模块名}
feature: {功能名}
status: draft
version: v1.0
author: {作者}
reviewer: {评审人}
created_at: {YYYY-MM-DD}
updated_at: {YYYY-MM-DD}
change_dir: changes/{功能名}
---

# {SPEC 标题}

## 1. 引言

### 1.1 目的与范围
{为什么要写这个 SPEC？解决什么问题？覆盖哪些功能？}

### 1.2 术语与缩略语

| 术语/缩略语 | 全称 | 定义 |
|------------|------|------|
| {术语} | {全称} | {在本文档中的含义} |

### 1.3 参考文献

| 编号 | 文档 | 版本 | 关联 |
|------|------|------|------|
| REF-1 | {文档名} | {版本} | {与本 spec 的关系} |

### 1.4 文档状态与变更历史

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|----------|------|
| v1.0 | {日期} | 初稿 | {作者} |

---

## 2. 系统概述

### 2.1 上下文图（系统边界）

```
[外部系统A] ──→ │              │ ──→ [外部系统B]
                │  {系统名称}  │
[用户/角色]  ──→ │  {功能范围}  │ ──→ [存储/服务]
                │              │
[上游依赖]   ──→ │              │ ──→ [下游影响]
```

### 2.2 核心概念模型

{描述系统中的核心概念、领域实体及其关系。如适用，使用两层积木、思维链等模型表达。}

### 2.3 利益相关方

| 角色 | 关注点 | 影响程度 |
|------|--------|----------|
| {角色名} | {关注什么} | 高/中/低 |

---

## 3. 功能性需求

> 编号规则：FR-{模块缩写}-{序号}，如 FR-DAG-001

### 3.1 {功能模块 1}

#### FR-{缩写}-001: {需求标题}
- **优先级**: P0/P1/P2
- **描述**: {详细描述}
- **输入**: {输入条件}
- **输出**: {预期输出}
- **规则**:
  1. {业务规则 1}
  2. {业务规则 2}

### 3.2 {功能模块 2}

#### FR-{缩写}-002: {需求标题}
- **优先级**: P0/P1/P2
- **描述**: {详细描述}

<!-- 按功能模块继续添加 FR-xxx -->

---

## 4. 非功能性需求

> 编号规则：NFR-{类别缩写}-{序号}，如 NFR-PERF-001

### 4.1 性能

| 编号 | 指标 | 目标值 | 测量方式 |
|------|------|--------|----------|
| NFR-PERF-001 | {指标名} | {目标值} | {测量方式} |

### 4.2 安全

| 编号 | 要求 | 标准 | 验证方式 |
|------|------|------|----------|
| NFR-SEC-001 | {安全要求} | {标准} | {验证方式} |

### 4.3 可用性

| 编号 | 指标 | SLA | 降级策略 |
|------|------|-----|----------|
| NFR-AVAIL-001 | {可用性要求} | {SLA} | {降级方案} |

### 4.4 可扩展性

| 编号 | 维度 | 上限 | 扩展策略 |
|------|------|------|----------|
| NFR-SCALE-001 | {扩展维度} | {上限} | {策略} |

---

## 5. 接口规范

### 5.1 REST API 契约

#### {接口名称}

| 属性 | 值 |
|------|-----|
| 方法 | GET/POST/PUT/DELETE |
| 路径 | `/api/v1/{resource}` |
| 描述 | {接口用途} |
| 认证 | {认证方式} |

**Request:**
```json
{
  "field1": "type",
  "field2": "type"
}
```

**Response (200):**
```json
{
  "code": 0,
  "data": {},
  "message": "success"
}
```

**Error Responses:**

| 状态码 | 错误码 | 描述 |
|--------|--------|------|
| 400 | {错误码} | {描述} |
| 401 | {错误码} | {描述} |

### 5.2 事件/消息契约

| 事件名 | 触发条件 | payload 模式 | 消费者 |
|--------|----------|-------------|--------|
| {事件名} | {触发条件} | `{json schema}` | {消费者} |

### 5.3 错误码表

| 错误码 | HTTP 状态码 | 描述 | 处理建议 |
|--------|------------|------|----------|
| ERR-{XXX} | {HTTP} | {描述} | {建议} |

---

## 6. 数据模型

### 6.1 实体关系图

```
[实体A] ──1:N──→ [实体B]
    │                  │
    │ 1:1              │ N:1
    ▼                  ▼
[实体C]           [实体D]
```

### 6.2 字段定义表

#### {实体名称}

| 字段名 | 类型 | 约束 | 默认值 | 说明 |
|--------|------|------|--------|------|
| id | {类型} | PK, NOT NULL | 自增 | 主键 |
| {字段} | {类型} | {约束} | {默认值} | {说明} |

<!-- 为每个实体重复此表 -->

---

## 7. 状态机

### 7.1 工作流生命周期状态图

```
  ┌──────┐      ┌───────┐      ┌──────────┐
  │ 状态A │ ──→ │ 状态B │ ──→ │  状态C   │
  └──────┘      └───┬───┘      └──────────┘
                    │                 │
                    │ 异常            │ 完成
                    ▼                 ▼
                 ┌──────┐        ┌────────┐
                 │ 状态D │        │ 状态E  │
                 └──────┘        └────────┘
```

### 7.2 状态迁移规则

| 当前状态 | 事件/条件 | 目标状态 | 副作用 |
|---------|----------|---------|--------|
| {状态A} | {触发事件} | {状态B} | {副作用描述} |

---

## 8. 验收标准

> 所有验收标准必须可度量、可验证

| 编号 | 验收项 | 通过标准 | 关联需求 | 优先级 |
|------|--------|----------|----------|--------|
| AC-001 | {验收项} | {可度量标准} | FR-{xxx} | P0/P1/P2 |
| AC-002 | {验收项} | Given/When/Then | FR-{xxx} | P0/P1/P2 |

### 端到端场景

| 场景 | 前置条件 | 操作步骤 | 预期结果 |
|------|----------|----------|----------|
| {场景名} | {前置条件} | 1. {步骤} 2. {步骤} | {预期结果} |

---

## 附录

### A. 实例配置示例 (YAML)

```yaml
# {配置用途说明}
{key}: {value}
{key}:
  - {item1}
  - {item2}
```

### B. 端到端场景走查

> 与第 8 节验收标准交叉引用，提供完整的前后文和断言。

#### 场景 B.1: {场景名}
- **前置**: {系统状态}
- **操作**: {步骤序列}
- **断言**:
  - [ ] {可观测结果 1}
  - [ ] {可观测结果 2}

### C. 实现参考（非规范正文）

> 代码片段仅供参考，不作为规范正文的一部分。实现可能随版本变化。

```{language}
// {参考说明}
{代码片段}
```
